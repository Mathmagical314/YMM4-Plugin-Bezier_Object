<UserControl x:Class="Curver.CurveEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Curver"
             mc:Ignorable="d" 
             d:DesignWidth="300" d:DesignHeight="200"
             Name="this">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tool bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,4">
            <Button Content="Open Window" Click="OpenWindow_Click" Padding="8,2" Background="#2D2D30" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <!-- Inline editor -->
        <local:CurveEditorControl x:Name="InlineEditor" Grid.Row="1" Height="150"/>
    </Grid>
</UserControl>
